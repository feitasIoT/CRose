name: webdav

services:
  app:
    container_name: crose_webdav
    image: 'webdav:test'
    ports:
     - '16065:6065'
    security_opt:
      - label=type:container_runtime_t
    restart: always
    volumes:
      - ./data:/data
      - ./config.yml:/config.yml:ro
